{
  "name": "extrafruit",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages-deploy",
    "clean-source": "rimraf README.md src webroot package.json"
  },
  "gh-pages-deploy": {
    "staticpath": "dist",
    "post": [
      "clean-source"
    ]
  },
  "homepage": "https://spencerduncan.github.io/extrafruit",
  "devDependencies": {
    "gh-pages": "^1.0.0"
  }
}
